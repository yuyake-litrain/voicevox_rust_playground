[package]
name = "voicevox_rust"
version = "0.1.0"
edition = "2024"

[dependencies]
const_format = "0.2.36"
voicevox_core = { git = 'https://github.com/VOICEVOX/voicevox_core', features = ['load-onnxruntime'] }
